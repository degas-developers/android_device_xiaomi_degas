# Batterysecret
vendor_restricted_prop(vendor_batterysecret_prop)

# Display
vendor_internal_prop(vendor_displayfeature_prop)

# Written by the fingerprint HAL, read by XiaomiParts, so it has to be public.
vendor_public_prop(vendor_degas_display_prop)

# Fingerprint
vendor_public_prop(vendor_fingerprint_prop)

# Mtdservice
vendor_public_prop(vendor_payment_security_prop)

# Panel identity, published by init.panel_info.sh
vendor_public_prop(vendor_panel_prop)

# Sensors
vendor_internal_prop(vendor_dynamic_sensor_prop)
