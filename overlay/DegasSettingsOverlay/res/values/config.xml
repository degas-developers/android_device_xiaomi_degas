<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<resources>
    <!-- The number of vibration intensity levels supported by the device.

         Note that this should correspond to the ability to vary the vibration amplitude, with
         enough dynamic range to have at least as many distinct intensity levels as defined here.

         Supported values are 1, 2, 3. If '1', the settings app will use a toggle for the settings,
         otherwise a slider. If '3', the settings app maps intensities directly to the levels
         supported by the Vibrator HAL APIs. If '2', then the levels will be mapped to
         VIBRATION_INTENSITY_LOW and VIBRATION_INTENSITY_HIGH, which gives the most range for
         scaling vibrations. The medium intensity will be skipped.

         The default intensity values are configured at
         frameworks/base/core/res/res/values/config.xml's config_default[type]VibrationIntensity.
         Make sure that each default intensity value is consistent with the supported levels set
         here. If this settings supports only 2 levels, for example, then the default intensity
         should be either LOW (1) or HIGH (3).
    -->
    <integer name="config_vibration_supported_intensity_levels">3</integer>

    <!-- Refresh rate, Display settings screen.

         The panel exposes all four rates as real physical modes (60 / 90 / 120 /
         144, verified in `dumpsys display` supportedModes and by setting
         Settings.System.peak_refresh_rate on hardware), so give the user the
         list instead of AOSP's Smooth Display toggle, which only ever picks
         between 60 and the top mode.

         Both write the same Settings.System.peak_refresh_rate, so exactly one of
         them must be shown.

         The min_refresh_rate list stays off on purpose. Pinning the floor to the
         top mode keeps the display permanently "at top", and that is the branch
         RefreshRateHealService uses to decide how to force the mode-set that
         undoes LHBM's silent drop to 120 Hz - with the floor pinned its peak
         nudge no longer moves anything and the fingerprint jank comes back
         (see parts/.../display/RefreshRateHealService.kt). -->
    <bool name="config_show_smooth_display">false</bool>
    <bool name="config_show_peak_refresh_rate_switch">true</bool>

    <!-- Display settings screen, Color mode options. Must be the same length and order as
         config_color_mode_options_values below. Only the values that also appear in
         frameworks/base/core/res/res/values/config.xml's config_availableColorModes are shown. -->
    <string-array name="config_color_mode_options_strings" translatable="false">
        <item>Standard</item>   <!-- standard -->
        <item>Vivid</item>      <!-- vivid -->
        <item>Saturated</item>  <!-- saturated -->
        <item>Original</item>   <!-- expert original -->
        <item>P3</item>         <!-- expert P3 -->
        <item>sRGB</item>       <!-- expert sRGB -->
    </string-array>

    <!-- Display settings screen, Color mode options. Must be the same length and order as
         config_color_mode_options_strings above. Only the values that also appear in
         frameworks/base/core/res/res/values/config.xml's config_availableColorModes are shown. -->
    <integer-array name="config_color_mode_options_values">
        <item>257</item> <!-- standard -->
        <item>258</item> <!-- vivid -->
        <item>256</item> <!-- saturated -->
        <item>269</item> <!-- expert original -->
        <item>268</item> <!-- expert P3 -->
        <item>267</item> <!-- expert sRGB -->
    </integer-array>

    <!-- The radius of the enrollment progress bar, in dp -->
    <integer name="config_udfpsEnrollProgressBar">90</integer>
</resources>
